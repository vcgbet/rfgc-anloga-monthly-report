{
  "name": "rfgc-anloga-monthly-report",
  "version": "1.0.0",
  "description": "Anloga District Rhema Full Gospel Churches Monthly Reporting Portal",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build",
    "render-build": "npm install && cd client && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "uuid": "^9.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
