services:
  - type: web
    name: rfgc-anloga-monthly-report
    env: node
    plan: free
    buildCommand: npm install && cd client && npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20.20.0
